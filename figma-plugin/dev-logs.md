● Giờ chạy plugin để tạo frames tự động:

  Bước 1 — Mở Figma Desktop → đăng nhập → mở file Untitled (hoặc tạo file mới)

  Bước 2 — Import plugin:
  - Menu Plugins → Development → Import plugin from manifest...
  - Chọn file: /home/ttthang/Practice/AI-first-todo-SaaS/figma-plugin/manifest.json

  Bước 3 — Chạy plugin:
  - Plugins → Development → TodoSaaS — Generate Frames
  - Chờ vài giây → 8 frames sẽ tự động được tạo
  
  Báo mình kết quả nhé!

✻ Churned for 5s

※ recap: Đang tạo Figma design cho TodoSaaS (issue THA-6). Plugin đã được viết sẵn tại `figma-plugin/`. 
  Bước tiếp theo: mở Figma Desktop → Plugins → Development → Import plugin from manifest → chọn file 
  manifest.json → chạy plugin. (disable recaps in /config)